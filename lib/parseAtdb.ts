/**
 * Shared domain types for `.atdb` parsing/building.
 * Kept as a compatibility layer for older imports.
 */

export { parseAtdb } from './sqlProcessor';
export type { Event, Family, ParsedAtdb, Person, Place } from './types';
